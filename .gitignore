# exclude the local secrets file
secrets.ps1

# exclude the autoload script where i set my $env:path's, etc.
locals.ps1

# exclude any autoload scripts or modules set to be ignored (leading underscore)
_*.ps1
_*.psm1
_*.psd1

# exclude the aws powershell helpers directory as that is a separate git repo
aws-powershell-helpers/

# exclude command history file
history.clixml

# exclude powertab config
PowerTabConfig.xml
TabExpansion.xml
